#!/bin/sh
if [ -d temp ]; then
    rm -rf temp/*
else
    mkdir temp
fi

cd temp || exit
./../getinfo.py "$1"
while IFS='' read -r link || [[ -n "$link" ]]; do
    wget -nv "$link"
done < "page_links"

